- name: default packages
  ansible.builtin.package:
    name:
      - apt-transport-https
      - curl
      - ca-certificates
      - mtr-tiny
      - nano
      - sshpass
      - wget
    state: latest
  become: yes
